# Site settings
title: WFP Editorial 
email: ""
description: ""
baseurl: "." # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site

exclude: ["*.sublime-*", "*.sh", "*.yml", "*.DS_*"]

# Build settings
markdown: kramdown
highlighter: pygments

kramdown:
  input: GFM

category-list: [Style Guidelines, Reference]

# Defaults
defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "default"

sass:
  style: expanded